package identifiers.common

import models.Index
import models.entities.{EntityType, PensionManagementType}

object utils {
  def getEntityTypeId(index: Index,
                      pensionManagementType: PensionManagementType,
                      entityType: EntityType) = {
    
  }
}
